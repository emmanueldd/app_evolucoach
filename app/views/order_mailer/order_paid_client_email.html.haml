%p
  Félicitations #{@client.first_name},
%p
  Ton paiement de :
  %b #{@order.item_title}
  (#{number_to_currency(@order.total_price.to_f / 100)}) est validé.

%table.btn.btn-primary{:border => "0", :cellpadding => "0", :cellspacing => "0", :role => "presentation"}
  %tbody
    %tr
      %td{:align => "left"}
        %table{:border => "0", :cellpadding => "0", :cellspacing => "0", :role => "presentation"}
          %tbody
            %tr
              %td
                = link_to 'Accueil', user_url(@user)
